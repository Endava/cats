auth:
  - name: cookie-user
    loginEndpointAuth:
      payloadRaw: "{}"

authTemplate:
  loginEndpointAuth:
    endpoint: /wfc-cookie-login
    verb: POST
    contentType: application/json
    expectCookies: true
